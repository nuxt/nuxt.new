export default defineAppConfig({
    ui: {
        primary: 'green',
        gray: 'zinc',

        popover: {
            popper: {
                placement: 'bottom-start'
            }
        }
    },
})
